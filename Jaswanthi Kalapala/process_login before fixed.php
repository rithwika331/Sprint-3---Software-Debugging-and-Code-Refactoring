<?php
if (password_verify($password, $user['password'])) {
            $_SESSION['login_message'] = "Login successful! Welcome back, " . $user['name'] . ".";
            $_SESSION['message_type'] = " failed"; // Message type for styling
            r
            
$conn->close();
?>
